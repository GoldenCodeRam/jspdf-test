import { mmToPoints } from './functions';

export const smallMarginSize = mmToPoints(10);
export const smallFooterMarginSize = mmToPoints(30);
export const smallHeaderMarginSize = mmToPoints(30);

export const normalHeaderMarginSize = mmToPoints(40);
export const normalFooterMarginSize = mmToPoints(40);

export const mediumHeaderMarginSize = mmToPoints(60);
export const mediumFooterMarginSize = mmToPoints(60);

export const largeHeaderMarginSize = mmToPoints(80);
export const largeFooterMarginSize = mmToPoints(80);

export const fontSizes = {
    tiny: 7,
    smaller: 8,
    small: 9,
    normal: 11,
};
